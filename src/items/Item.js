export class Item {
  constructor({ name, description, icon, playericon, sfx }) {
    this.name = name;
    this.description = description;
    this.icon = icon;
    this.playericon = playericon;
    this.sfx = sfx;
  }
}
